[package]
name = "matrix-multiplication"
version = "0.1.0"
edition = "2024"

[dependencies]
cust = { path = "../crates/cust" }

[build-dependencies]
cuda_builder = { workspace = true, default-features = false }
